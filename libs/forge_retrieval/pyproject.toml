[project]
name = "forge-retrieval"
version = "12.0.0"
description = "Forge Retrieval — Hybrid Pipeline (Qdrant + SQLite FTS5 + Cross-Encoder Reranker)"
requires-python = ">= 3.12"
dependencies = [
    "qdrant-client>=1.11.0",
    "pydantic>=2.9.0",
    "httpx>=0.27.0",
]
