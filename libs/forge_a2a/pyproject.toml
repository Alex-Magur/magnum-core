[project]
name = "forge-a2a"
version = "12.0.0"
description = "Forge A2A — Agent-to-Agent Protocol Support (Dual Discovery, Agent Card)"
requires-python = ">= 3.12"
dependencies = [
    "cryptography>=43.0.0",
    "pydantic>=2.9.0",
]
